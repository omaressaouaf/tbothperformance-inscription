export default {
    data() {
        return {
            _baseUrl: window._baseUrl,
            _appName: window._appName,
            _locale: window._locale,
            _supportedLocales: window._supportedLocales,
            _translations: window._translations,
        };
    },
};
